<Window x:Class="WorldCup.WPF.TeamDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorldCup.WPF"
        mc:Ignorable="d"
        Title="Team Details" Height="400" Width="600"
        Loaded="Window_Loaded"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="White"
        Opacity="0">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock x:Name="txtCountry" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock x:Name="txtCode"/>
            <TextBlock x:Name="txtPlayed"/>
            <TextBlock x:Name="txtWins"/>
            <TextBlock x:Name="txtLosses"/>
            <TextBlock x:Name="txtDraws"/>
            <TextBlock x:Name="txtGoalsScored"/>
            <TextBlock x:Name="txtGoalsConceded"/>
            <TextBlock x:Name="txtGoalDiff"/>
        </StackPanel>

        <!-- Buttons at the bottom -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Close" Width="100" Click="Button_Click"/>
        </StackPanel>
    </Grid>
</Window>
